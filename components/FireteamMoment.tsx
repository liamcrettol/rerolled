// Landing-page value proposition, kept separate so the copy remains easy to
// reuse without imposing layout or typography on its parent.
export default function FireteamMoment() {
  return <h2>Built from what your fireteam actually owns.</h2>;
}
